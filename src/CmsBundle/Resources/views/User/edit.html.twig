{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <h1>User edit <span class="tooltipped" data-position="bottom" data-delay="30" data-tooltip="Admin = delete and all, EDITOR = validation & stage show , stage number & thesaurus, Expert = persons & films, MEMBER  = number & song, USER = cannot edit, just access to thematics pages, etc. "><i class="material-icons circle white-text grey lighten-1">help</i></span></h1>
        {{ form_start(edit_form) }}
            {{ include('CmsBundle:User:_form.html.twig')}}
        <input type="submit" class="btn btn-admin green right" value="Edit" />
        {{ form_end(edit_form) }}

        {{ form_start(delete_form) }}
        <input type="submit" class="btn btn-admin red right" value="Delete">
        {{ form_end(delete_form) }}

        <a href="{{ path('admin') }}">Back to the list</a>

        <br><br>

    </div>
{% endblock %}
