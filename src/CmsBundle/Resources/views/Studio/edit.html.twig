{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">

        <h1>Studio edit</h1>

        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" class="btn btn-admin green right" value="Edit" />
        {{ form_end(edit_form) }}

        {{ form_start(delete_form) }}
        <input type="submit" class="btn btn-admin red right" value="Delete">
        {{ form_end(delete_form) }}

        <a href="{{ path('admin_studio_index') }}">Back to the list</a>

        <br><br>

    </div>
{% endblock %}
